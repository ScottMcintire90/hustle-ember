import { controlsAutocomplete } from 'hustle/helpers/controls/autocomplete';
import { module, test } from 'qunit';

module('Unit | Helper | controls/autocomplete');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = controlsAutocomplete([42]);
  assert.ok(result);
});
