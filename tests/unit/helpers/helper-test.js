import { helper } from 'hustle/helpers/helper';
import { module, test } from 'qunit';

module('Unit | Helper | helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helper([42]);
  assert.ok(result);
});
