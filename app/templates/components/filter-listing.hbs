Article Name: {{input value=filter key-up=(action 'autoComplete' filter)}}
<button {{action 'search'}}>Search</button>

<ul>
  {{#each filteredList as |article|}}
    <li {{action 'choose' article.name}}>{{article.name}}</li>
  {{/each}}
</ul>
